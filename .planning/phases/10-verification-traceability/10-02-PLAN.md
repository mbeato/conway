---
phase: 10-verification-traceability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-stripe-billing/05-VERIFICATION.md
  - .planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05
  - FE-06
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
  - INT-06
  - INT-07
  - INFRA-03
must_haves:
  truths:
    - "Phase 5 has a VERIFICATION.md confirming BILL-01 through BILL-05 and FE-06 are satisfied"
    - "Phase 7 has a VERIFICATION.md confirming INT-01 through INT-07 and INFRA-03 are satisfied"
  artifacts:
    - path: ".planning/phases/05-stripe-billing/05-VERIFICATION.md"
      provides: "Verification report for Phase 5"
      contains: "status: passed"
    - path: ".planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md"
      provides: "Verification report for Phase 7"
      contains: "status: passed"
  key_links: []
---

<objective>
Create VERIFICATION.md files for Phase 5 (Stripe Billing) and Phase 7 (API Key Auth Middleware).

Purpose: These phases were completed without verification reports. This plan audits actual source code against requirements and creates verification artifacts.
Output: 2 VERIFICATION.md files with code-level evidence for all requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-verification-traceability/10-RESEARCH.md
@.planning/phases/03-auth-hardening-sessions/03-VERIFICATION.md

<interfaces>
<!-- Phase 5 requirements and source locations: -->
<!-- BILL-01: shared/stripe.ts has CREDIT_TIERS with 4 tiers ($5/$20/$50/$100) -->
<!-- BILL-02: shared/stripe.ts CREDIT_TIERS has bonusPercent: 0/10/20/30 -->
<!-- BILL-03: apis/dashboard/index.ts webhook grants credits, not client redirect -->
<!-- BILL-04: shared/credits.ts addCredits() has UNIQUE constraint on stripe_payment_intent -->
<!-- BILL-05: shared/stripe.ts verifyWebhookSignature() with timingSafeEqual + 5min age check -->
<!-- FE-06: apis/landing/billing.html exists with tier cards -->

<!-- Phase 7 requirements and source locations: -->
<!-- INT-01: shared/api-key-auth.ts apiKeyAuth() accepts Bearer sk_live_... -->
<!-- INT-02: apis/router.ts single insertion point before subApp.fetch() -->
<!-- INT-03: shared/credits.ts deductAndRecord() uses BEGIN IMMEDIATE -->
<!-- INT-04: shared/api-key-auth.ts returns 402 with balance info -->
<!-- INT-05: shared/api-key-auth.ts returns 401 for invalid/revoked -->
<!-- INT-06: shared/api-key-auth.ts returns null for no Bearer (x402 fallthrough) -->
<!-- INT-07: apis/router.ts or api-key-auth.ts sets X-Credits-Remaining header -->
<!-- INFRA-03: caddy/Caddyfile has @auth_paths with Cache-Control: no-store -->

<!-- Note: INFRA-04 was originally Phase 5 but completed in Phase 9. Do NOT include in Phase 5 verification. -->
<!-- Note: INT-08 was originally Phase 7 but completed/fixed in Phase 9. Do NOT include in Phase 7 verification. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5 Stripe Billing VERIFICATION.md</name>
  <files>.planning/phases/05-stripe-billing/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-auth-hardening-sessions/03-VERIFICATION.md (format reference)
    - .planning/phases/05-stripe-billing/05-01-SUMMARY.md
    - .planning/phases/05-stripe-billing/05-02-SUMMARY.md
    - .planning/phases/05-stripe-billing/05-03-SUMMARY.md
    - shared/stripe.ts (BILL-01: CREDIT_TIERS, BILL-02: bonusPercent, BILL-05: verifyWebhookSignature)
    - apis/dashboard/index.ts (BILL-03: webhook route, BILL-04: addCredits call)
    - shared/credits.ts (BILL-04: addCredits with idempotency)
    - apis/landing/billing.html (FE-06: billing page)
  </read_first>
  <action>
Create `.planning/phases/05-stripe-billing/05-VERIFICATION.md` following the exact format of `03-VERIFICATION.md`.

YAML frontmatter fields: phase=05-stripe-billing, verified=2026-03-24T00:00:00Z, status=passed, score="5/5 must-haves verified", re_verification=false.

Phase Goal: "Users can purchase credits via Stripe Checkout with webhook-confirmed grants"

Observable Truths (from ROADMAP success criteria):
1. "User can click a tier card ($5/$20/$50/$100) and complete purchase through Stripe Checkout" - verify shared/stripe.ts CREDIT_TIERS array has 4 entries, POST /billing/checkout creates session
2. "Volume bonuses are applied correctly (0%/10%/20%/30% per tier)" - verify CREDIT_TIERS bonusPercent values and credit amounts reflect bonuses
3. "Credits appear in user's balance only after webhook confirmation" - verify POST /billing/webhook calls addCredits, POST /billing/checkout does NOT add credits
4. "Processing same Stripe payment_intent twice does not double-grant" - verify addCredits has UNIQUE constraint or ON CONFLICT handling on stripe_payment_intent
5. "Webhook signature verified with timing-safe comparison, events >5min rejected" - verify verifyWebhookSignature uses crypto.timingSafeEqual and checks timestamp age > 300

Required Artifacts: shared/stripe.ts, apis/dashboard/index.ts (webhook + billing routes), shared/credits.ts (addCredits), apis/landing/billing.html

Requirements Coverage table:
- BILL-01: CREDIT_TIERS in shared/stripe.ts has 4 tiers; POST /billing/checkout creates Stripe session with tier metadata
- BILL-02: CREDIT_TIERS bonusPercent values are 0, 10, 20, 30; credit amounts are 500000, 2200000, 6000000, 13000000
- BILL-03: POST /billing/webhook grants credits via addCredits(); POST /billing/checkout only creates session, no credit grant
- BILL-04: addCredits() in shared/credits.ts uses INSERT with UNIQUE constraint on stripe_payment_intent column
- BILL-05: verifyWebhookSignature() in shared/stripe.ts uses crypto.timingSafeEqual for HMAC comparison, rejects events where (currentTime - t) > 300
- FE-06: apis/landing/billing.html exists with 4 tier cards, balance display, buy buttons; served at GET /account/billing

Note: INFRA-04 (webhook route with separate handler) was originally in Phase 5 requirements but was completed and verified in Phase 9. Do NOT include it here.

Gaps Summary: None. All Phase 5 requirements satisfied.
  </action>
  <verify>
    <automated>grep -c "status: passed" .planning/phases/05-stripe-billing/05-VERIFICATION.md && grep -c "BILL-05" .planning/phases/05-stripe-billing/05-VERIFICATION.md && grep -c "FE-06" .planning/phases/05-stripe-billing/05-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/05-stripe-billing/05-VERIFICATION.md exists
    - File contains "status: passed" in YAML frontmatter
    - File contains "phase: 05-stripe-billing" in YAML frontmatter
    - File contains "score: 5/5 must-haves verified"
    - File contains all 6 requirement IDs: BILL-01, BILL-02, BILL-03, BILL-04, BILL-05, FE-06
    - File does NOT contain INFRA-04 (that was Phase 9)
    - File has Observable Truths table with 5 rows, all VERIFIED
    - File has Requirements Coverage table with SATISFIED for each requirement
    - File references actual file paths and line numbers as evidence
  </acceptance_criteria>
  <done>Phase 5 VERIFICATION.md exists with passed status, all 6 requirements verified against actual source code</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 7 API Key Auth Middleware VERIFICATION.md</name>
  <files>.planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-auth-hardening-sessions/03-VERIFICATION.md (format reference)
    - .planning/phases/07-api-key-auth-middleware/07-01-SUMMARY.md
    - .planning/phases/07-api-key-auth-middleware/07-02-SUMMARY.md
    - .planning/phases/07-api-key-auth-middleware/07-03-SUMMARY.md
    - shared/api-key-auth.ts (INT-01: Bearer auth, INT-04: 402 response, INT-05: 401 response, INT-06: null fallthrough)
    - shared/x402.ts (INT-06: payment bypass for internal auth)
    - apis/router.ts (INT-02: single insertion point, INT-07: X-Credits-Remaining header)
    - shared/credits.ts (INT-03: atomic deduction with BEGIN IMMEDIATE)
    - caddy/Caddyfile (INFRA-03: @auth_paths with Cache-Control: no-store)
  </read_first>
  <action>
Create `.planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md` following the exact format of `03-VERIFICATION.md`.

YAML frontmatter fields: phase=07-api-key-auth-middleware, verified=2026-03-24T00:00:00Z, status=passed, score="5/5 must-haves verified", re_verification=false.

Phase Goal: "All 21 APIs accept API key authentication as an alternative to x402, with atomic credit deduction"

Observable Truths (from ROADMAP success criteria):
1. "Sending Authorization: Bearer sk_live_... to any API deducts credits and returns response" - verify apiKeyAuth() in shared/api-key-auth.ts processes Bearer tokens, calls deductAndRecord
2. "No Bearer header falls through to existing x402 flow (zero breaking changes)" - verify apiKeyAuth() returns null when no Bearer header; router.ts only acts on non-null response
3. "Insufficient credits returns 402 with balance info; invalid/revoked key returns 401" - verify error responses in apiKeyAuth()
4. "API responses include X-Credits-Remaining header for API key users" - verify header set in router.ts or api-key-auth.ts response
5. "Middleware inserted at one place in the router (not 21 separate insertions)" - verify apis/router.ts has single apiKeyAuth() call in catch-all route

Required Artifacts: shared/api-key-auth.ts, shared/x402.ts, apis/router.ts, caddy/Caddyfile

Key Link Verification:
- shared/api-key-auth.ts -> shared/api-key.ts via lookupByHash() for key validation
- shared/api-key-auth.ts -> shared/credits.ts via deductAndRecord() for credit deduction
- apis/router.ts -> shared/api-key-auth.ts via apiKeyAuth() call in catch-all
- shared/x402.ts -> internal auth bypass via INTERNAL_AUTH_SECRET and X-APIMesh-Internal header
- caddy/Caddyfile -> @auth_paths matcher for /auth/*, /account*, /billing/* with Cache-Control: no-store

Requirements Coverage table:
- INT-01: apiKeyAuth() in shared/api-key-auth.ts extracts Bearer token, validates sk_live_ prefix, looks up hash
- INT-02: apis/router.ts has single apiKeyAuth() call in catch-all route before subApp.fetch()
- INT-03: deductAndRecord() in shared/credits.ts uses db.transaction().immediate() for BEGIN IMMEDIATE
- INT-04: apiKeyAuth() returns Response with status 402 and JSON body containing balance_microdollars when credits insufficient
- INT-05: apiKeyAuth() returns Response with status 401 for invalid key hash lookup or revoked key
- INT-06: apiKeyAuth() returns null when Authorization header missing or not Bearer sk_live_*; router.ts checks for null before using response
- INT-07: Response from apiKeyAuth() or router includes X-Credits-Remaining header
- INFRA-03: caddy/Caddyfile has @auth_paths path matcher with header_down Cache-Control "no-store"

Note: INT-08 (request logging with user_id/api_key_id) was originally Phase 7 but was fixed in Phase 9. Do NOT include it here.

Gaps Summary: None. All Phase 7 requirements satisfied.
  </action>
  <verify>
    <automated>grep -c "status: passed" .planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md && grep -c "INT-07" .planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md && grep -c "INFRA-03" .planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/07-api-key-auth-middleware/07-VERIFICATION.md exists
    - File contains "status: passed" in YAML frontmatter
    - File contains "phase: 07-api-key-auth-middleware" in YAML frontmatter
    - File contains "score: 5/5 must-haves verified"
    - File contains all 8 requirement IDs: INT-01, INT-02, INT-03, INT-04, INT-05, INT-06, INT-07, INFRA-03
    - File does NOT contain INT-08 (that was Phase 9)
    - File has Observable Truths table with 5 rows, all VERIFIED
    - File has Key Link Verification table showing wiring between api-key-auth.ts, x402.ts, router.ts, credits.ts, Caddyfile
    - File has Requirements Coverage table with SATISFIED for each requirement
  </acceptance_criteria>
  <done>Phase 7 VERIFICATION.md exists with passed status, all 8 requirements verified against actual source code</done>
</task>

</tasks>

<verification>
- 05-VERIFICATION.md exists with status: passed and covers BILL-01 through BILL-05 and FE-06
- 07-VERIFICATION.md exists with status: passed and covers INT-01 through INT-07 and INFRA-03
- Both verification files follow the format established by 03-VERIFICATION.md
- Evidence in both files references actual source files with line-level detail
</verification>

<success_criteria>
- 2 new VERIFICATION.md files created (phases 5 and 7)
- All verification reports show status: passed with file:line evidence
- Every requirement ID listed is mapped to a source code artifact with specific evidence
- No requirement IDs from other phases are incorrectly included
</success_criteria>

<output>
After completion, create `.planning/phases/10-verification-traceability/10-02-SUMMARY.md`
</output>
